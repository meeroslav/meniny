{
  "root": "libs/calendar-data",
  "sourceRoot": "libs/calendar-data/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/calendar-data/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/calendar-data"],
      "options": {
        "jestConfig": "libs/calendar-data/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
